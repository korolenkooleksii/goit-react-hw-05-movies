// import { Outlet } from "react-router-dom";

import { SearchBar } from 'components/SearchBar';

const Movies = () => {
  return (
    <main>
      <SearchBar />
    </main>
  );
};

export default Movies;
